{
  "name": "grabit-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mobile"
  ],
  "scripts": {
    "start": "npm start --prefix mobile",
    "android": "npm run android --prefix mobile",
    "build": "npm install --prefix mobile --legacy-peer-deps && npm run build --prefix mobile"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.20.2",
    "@types/react": "~19.2.4",
    "typescript": "~6.0.3"
  }
}
